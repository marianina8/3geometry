[build]
  command = "npm install --legacy-peer-deps --verbose && npm run build"
  publish = "build"

[build.environment]
  NPM_CONFIG_REGISTRY = "https://registry.npmjs.org/"
  NODE_VERSION = "18"
  NODE_OPTIONS = "--max_old_space_size=4096"
